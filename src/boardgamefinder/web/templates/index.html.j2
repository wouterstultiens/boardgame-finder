<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Board Game Finder</title>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; line-height: 1.6; color: #333; max-width: 960px; margin: 2rem auto; padding: 0 1rem; background-color: #f8f9fa; }
        h1 { text-align: center; color: #0056b3; }
        .game-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 1.5rem; }
        .game-card { background-color: #fff; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); overflow: hidden; display: flex; flex-direction: column; }
        .game-card img { width: 100%; height: 200px; object-fit: cover; }
        .game-info { padding: 1rem; flex-grow: 1; }
        .game-title { font-size: 1.25rem; font-weight: bold; margin: 0 0 0.5rem; }
        .game-title a { text-decoration: none; color: #0056b3; }
        .game-title a:hover { text-decoration: underline; }
        .game-meta { display: flex; justify-content: space-between; font-size: 0.9rem; color: #555; margin-bottom: 1rem; }
        .game-listings { border-top: 1px solid #eee; padding-top: 1rem; }
        .listing { display: flex; justify-content: space-between; align-items: center; padding: 0.5rem 0; font-size: 0.9rem; }
        .listing a { background-color: #e9ecef; padding: 0.25rem 0.75rem; border-radius: 12px; text-decoration: none; color: #333; font-weight: 500; }
        .listing a:hover { background-color: #ced4da; }
        footer { text-align: center; margin-top: 3rem; color: #777; font-size: 0.8rem; }
    </style>
</head>
<body>
    <header>
        <h1>Board Game Finder</h1>
    </header>
    <main class="game-grid">
        {% for game in games %}
        <div class="game-card">
            {% if game.image %}
            <img src="{{ game.image }}" alt="{{ game.name }}">
            {% endif %}
            <div class="game-info">
                <div class="game-title">
                    <a href="{{ game.bgg_link }}" target="_blank" rel="noopener noreferrer">{{ game.name }}</a>
                </div>
                <div class="game-meta">
                    <span><strong>Rating:</strong> {{ "%.2f"|format(game.rating) }}</span>
                    <span><strong>Weight:</strong> {{ "%.2f"|format(game.weight) }}</span>
                    <span><strong>Year:</strong> {{ game.year }}</span>
                </div>
                <div class="game-listings">
                    <strong>Listings:</strong>
                    {% for listing in game.listings %}
                    <div class="listing">
                        <span>€{{ "%.2f"|format(listing.price) }} in {{ listing.city }}</span>
                        <a href="{{ listing.link }}" target="_blank" rel="noopener noreferrer">View on Marktplaats</a>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
        {% else %}
        <p>No games found matching the criteria.</p>
        {% endfor %}
    </main>
    <footer>
        <p>Last updated: {{ now().strftime("%Y-%m-%d %H:%M:%S") }} UTC</p>
    </footer>
</body>
</html>